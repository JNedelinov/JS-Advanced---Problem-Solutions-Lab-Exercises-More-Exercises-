function solve() {
  let rightAnswers = 0;

  let sections = Array.from(document.querySelectorAll("sections"));
  for (let i = 0; i < sections.length; i++) {
    console.log(sections[i]);
  }
  
}
