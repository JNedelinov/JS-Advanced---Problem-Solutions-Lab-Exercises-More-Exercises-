
        /* seconds < 10 && minutes < 10 ? `0${minutes}:0${seconds}`: `${minutes}:${seconds}`; */
        
        /* seconds++;
        let num = +document.getElementById("time").textContent.split(":")[1] + 1;
        if (num < 60) {
            if (num < 10) {
                document.getElementById("time").textContent = `${minutes}:0${num}`;
            } else {
                document.getElementById("time").textContent = `${minutes}:${num}`;
            }
        } else {
            num = 0;
            minutes++;
            if (minutes < 10) {
                document.getElementById("time").textContent = `0${minutes}:0${num}`;
            } else {
                document.getElementById("time").textContent = `${minutes}:0${num}`;
            }
        } */